# ======================================
# Core Application Frontend - Environment Config
# ======================================
# Copy this file to .env and fill in your values
# IMPORTANT: Never commit .env to version control!
#
# Stories:
#   - STORY-001: Login System Frontend
#   - STORY-008: Session Management mit "Remember Me"
#   - STORY-009: Password Reset
#   - STORY-007B: User Role Assignment
#   - STORY-017A: Layout & Grid-System (Tailwind CSS, responsive breakpoints)
#   - STORY-030: Application Versioning (Version display in footer and About dialog)
#
# Breakpoints (STORY-017A):
#   - sm: 640px  (small tablets, large phones landscape)
#   - md: 768px  (tablets, mobile breakpoint)
#   - lg: 1024px (small desktops, tablets landscape)
#   - xl: 1280px (desktop breakpoint)
#
# CSS Custom Properties (STORY-017A):
#   Primary colors are defined in src/styles/globals.css as CSS custom properties
#   (--color-primary, --color-secondary, etc.) and mapped to Tailwind config
#
# Layout Components (STORY-017A):
#   - Container: Max-width wrapper with responsive padding
#   - Row: Flex container for grid rows with gap support
#   - Col: 12-column grid system with responsive spans
#   - Grid/GridItem: CSS Grid based layout for card grids

# ----------------
# Application
# ----------------
# Environment: development | production
NODE_ENV=development

# API URL - Backend server endpoint
VITE_API_URL=http://localhost:4102/api

# ----------------
# E2E Testing (Playwright)
# ----------------
# Frontend URL for Playwright tests
# Defaults to http://localhost:3000 in playwright.config.ts
FRONTEND_URL=http://localhost:3000

# ----------------
# Application Versioning (STORY-030)
# ----------------
# Application version (defaults to package.json version if not set)
# Can be overridden for build-time version injection
VITE_APP_VERSION=

# ----------------
# Feature Flags (Optional)
# ----------------
# Enable/disable features for testing
# VITE_ENABLE_MFA=true
# VITE_ENABLE_DARK_MODE=false
